function setup() {
  createCanvas(1200, 720); // draw the working canvas
}

function draw() {
  background(245, 230, 245); // light pink background in draw to keep it refreshing
}

function monster(a, b) { // don't forget to include some parameters, this is the larger monster function that will call smaller body part functions
  
}